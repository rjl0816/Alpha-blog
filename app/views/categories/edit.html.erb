<h1 class="text-center mt-5 mb-5">Edit Category</h1>

<div class="row justify-content-center">
    <div class="col-10">
        <%= render 'form' %>
        <%= link_to 'Cancel', category_path(@category), class: "row justify-content-center mt-5 text-danger" %>
    </div>
</div>